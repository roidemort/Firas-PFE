import { AppPhoneNumberDirective } from './app-phone-number.directive';

describe('AppPhoneNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPhoneNumberDirective();
    expect(directive).toBeTruthy();
  });
});
